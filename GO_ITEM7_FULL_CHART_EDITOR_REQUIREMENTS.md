# 第七大項追加：完整圖表編輯系統（2026-09-25）

使用者實機回報：00406A／00878／00919 的真實 TWSE 日 K 已顯示；009816 六位數代號在舊版遭擋；現有 K 線日期座標重疊，圖表可編輯能力與圖表渲染不一致。

## 圖表編輯器定義
- 入口：設定 → 圖表系統；首頁儀表板右上圖表、ETF 詳情 K 線、持股行情圖表、庫存分析及股息頁，均有對應就地編輯入口。
- 本專案只使用現有逐層收合 A→B→C→D→E 編輯規則，不引入 360。A＝實際圖表卡框架；B＝類型、資料、外觀、座標、互動、版面；必要時 C/D/E 展開細項，切換組別自動收合。
- 每個圖表卡獨立儲存：顯示／隱藏、標題／副標、圖表類型、資料來源、標的／區間、排序、圖層、位置、大小、鎖定、比例、透明度、圓角、邊框、陰影、圖例、工具提示、更新來源時間。
- 圖表語義真實：折線、面積、柱、堆疊、圓餅、環形、資產配置、損益、報酬、股息、真實 OHLC K 線與成交量均由匹配資料源與適當 renderer 真正繪製，不可把 sparkline 字元、單價或持股清單錯充歷史 K 線或時間序列。資料不足顯示原因與待取得，提供原始來源資訊。
- K 線專屬：開高低收、成交量、價格／日期座標、週期、十字線、觸控讀值、雙擊放大、雙指縮放、平移、重設與放大視窗；歷史來源不可用時明確顯示，未驗證的分時／週 K 不得假展示。
- X 軸依寬度自動疏排標籤、按月／週刻度、避免日期擠壓；Y 軸價格精度與成交量單位自適應；小卡／放大／全螢幕各自驗證。
- 顏色：全項使用現有 Color Picker，任何文字／背景／K 棒／成交量／邊框皆可切換引用系統漲跌中性色，預設沿用使用者已設定損益色；不得提供手輸色碼作為唯一設定。
- 浮動即時預覽：單一完整圖表卡小窗，可拖曳、縮放、最小化與關閉；編輯僅寫草稿，按「套用」後保存，取消回復既有設定。預覽使用同一 renderer，不可截圖或假圖。
- 不得變動 immutable core 或重新計算帳務公式，圖表只讀既有 canonical projection / official price series。
- 新增／移除圖表、重排、跨卡置頂置底、跨頁相同圖表能力矩陣；保留各頁獨立版面設定。
- 必須具備互動測試、資料來源與空態測試、各頁功能路徑測試及 Android 裝置 UI／手勢驗收，CI PASS 不等於功能 PASS。

## 本輪狀態
以上為使用者追加且已記錄的驗收規格；尚未完成的圖表渲染及編輯器不得標 PASS。009816 與日期擠壓列同一第七大項修護批次；實際程式更改及新 QA APK 須依下一輪版本遞增及 GO gate 另行記錄。
