# V3.0.17 進階技能接線及剩餘驗證表

| 能力 | C 層實際操作 | 原始資料保護 | 驗證邊界 |
| --- | --- | --- | --- |
| 多選批次編輯 | 勾選當前框架實際已掛載元件、明確視覺欄位、逐目標差異預覽、暫存批次 | 只寫入 `normalizeTargetOverride` 允許的視覺欄位；local-only 持續保護明確覆寫 | 真實手機觸控與版面仍待使用者測試 |
| 局部視覺差異比較 | 將當前 A 已儲存／繼承樣式與暫存草稿逐欄對照 | 略過交易金額、費稅、股數與資料來源 | 只能證明当前已註冊元件與框架的差異 |
| 畫面健康診斷 | 顯示目前已量測元件的越界、同類疑似重疊與小觸控區 | 唯讀，不自動調整框架／子元件 | 未量測 ≠ PASS；手機實機仍需確認 |

技能樹沿用 V3.0.16 使用者確認的 AB 屬性定律，B 下直接顯示 C；六組專業分類及 30 類分類僅作中央索引與報告使用。

**稽核目標**：184 項＝143 宣告接入＋41 待原生接線（其中 3 項本輪由待接線轉為接入）。CI 產出 `reports/V3.0.17-full-skill-matrix.json`，其聲明只是源碼／自動化覆蓋，非實機 PASS。

**下一批仍未完成**：真正的效果堆疊與原生渲染、設計變數與主題引用、多視覺狀態、可存取性友善的動畫、中央模板安全複用、專業圖表與 Widget／AI 原生介接。選擇下一批時需保持小範圍、每項驗證后產出正式遞增的 QA APK。
