# TF Asset｜維護工程師全技能整合與缺口追蹤（V3.0.14）

本檔是執行規格與可驗證清單，不以技能樹可見代替功能實作。分類命名依最近一次 30 大類整合提案；若需逐字還原更早版本的核准名稱，必須另以原始紀錄核實，不能宣稱已找回。

## 核心架構

A 當前真實框架／元件 → B 完整 30 類 → C 共用專業工具 → D 具體編輯參數。30 類由 6 組導覽排列，任何 A 對象都能看到 30 類與全部 C 工具，僅當前原生渲染介面可用性不同。背景、文字漸層、圖表線色等共享中央技能定義，但各自渲染器須有獨立測試；若無對應渲染器，只能明示「原生適配待完成」，禁止假接線與假成功提示。

## 不可遺漏的範圍

* 現有 169 項：140 項宣告已接線但尚需按元件逐項實測；29 項原定待接入。工具原始 ID 保留以保障舊版儲存及回歸。
* 必須追蹤的 12 項：多選批次編輯、效果堆疊、設計變數、條件樣式、視覺狀態、圖層面板、可視化事件、資料模擬預覽、局部差異比較、畫面健康診斷、技能搜尋收藏、全局適配驗證台。現階段尚未完整原生接通的能力不能標記為 PASS。
* 尚未有中央專用技能入口的 3 個領域：頁面／導覽、Widget／Monitor、AI／通知。須繼續補齊真正的 App 元件適配。
* 合計目前中央追蹤 184 個技能／缺口項目，其中 44 項明確待實作／適配；另有「宣告 ready 但尚未通過原生或實機驗收」的待驗風險，未完成總數可能高於 44。

## 防止回歸的必備 Gate

1. 3.0.12 的原本正常畫面與控件布局、原地真實預覽、保存與回復行為保留。
2. 所有 169 個原始 ID 必須各自映射到唯一 B 類，30 類全部非空，12 項專業能力全部存在且狀態誠實。
3. 執行 11 類元件 × 169 個既有工具 = 1859 列靜態適配契約驗算，產出可下載 JSON；此僅為接線契約，不取代真實渲染測試。
4. 行為為 locked 的框架仍可調整非數據視覺。僅帳務核心、真實來源及不可替換的金額受保護；原生元件不可刪除。
5. 逐項驗證當前元件可操作、真實預覽、取消復原、正式套用、重啟保留、跨頁隔離及同步作用範圍；缺證據者始終為待驗，不得列為 ALL PASS。
6. TypeScript、歷版回歸、Node/Redis/PostgreSQL、Expo、Android QA APK、SHA、badging 和手機實機驗收分開回報。

實際逐工具輸出：`reports/V3.0.14-full-skill-matrix.json`（GitHub Action 自動產出與上傳）。
